# Critique — spec-layer0 (pre-impl lock-in)

- **Date:** 2026-06-13
- **Target reference:** `spec-critique` (pre-impl spec lock-in)
- **Change under review:** `docs/spec-layer0.md` — Layer 0 + validation harness build contract
- **Execution:** ran (bounded fresh-eye subagents)
- **Fresh-Eye Satisfaction:** `nested-delegated` (spec → critique parent delegated to fresh-eye subagents)
- **Packet Consumed:** n/a (no critique adapter; `packet_sections` empty)
- **Angles:** Minto (structure) · Jackson (problem-framing) · Weinberg (diagnostic) · Gawande (operational) + 1 separate counterweight pass

## Coherence / Coverage (original draft)

- **Fixed/Probe/Defer coherence:** FAIL → fixed. F6 hid the split-`T` selection knob; P2/P3 named no write-back location; several Deferred items had no reopen trigger.
- **Acceptance-check coverage:** FAIL → fixed. The mute-map risk (ambiguous swallowing most boundaries) had a Probe + Constraint but no Success Criterion and no check — only a non-zero lower-bound on `ambiguous`.

## Counterweight four-bin triage

**Act Before Ship (applied to spec):**
- C1 — `T` is an unconstrained knob → pre-registered mechanical rule (70/30 split), verdict states Z is not a T-sweep max. (F6)
- C2 — mute-map not guarded by the number → coverage denominator + mute-gate ceiling. (F12, SC2/SC5)
- C4 — equal-budget unit wrong (LOC) + missing dumb baseline → budget by #functions; add "all functions with any boundary call" null baseline. (F7)
- C6 — repo-fit vs tool-death conflation → two-axis verdict in kill-gate.md. (F13)
- C7 — promote repo-fit to an analyzer-free FIRST gate → Slice 0 / SC0 / P1.
- C8 — mining-recall blindness → P1b LLM-classify a sample of non-matched bugfix commits.
- C10 — join under-specified → explicit key = file path + qualified function name at `T`. (F11)
- C11 — SC1 freeze incomplete → freeze the SZZ-resolved bug-site set, not just labels. (F9)
- (cheap substitutes) C3 → raw n + pre-registered n-floor (no CI math); C5 → `git blame -w` + conservative-bias note. (F14)

**Bundle Anyway (applied):**
- C12 — labeler doctor/precondition + cost ceiling + resumable cached writes.
- C13 — move negative-control/welded-twin fixtures from Slice 0 to Slice 2 (they presuppose the seam/weld definition).
- C14 — Cargo scaffold + `pry --version` + 3-line `pry map` smoke before the expensive join.

**Valid but Defer (recorded with reopen triggers):**
- C9 — non-circular negative control (F% on charness functions pure by independent signal). Reopen when F% drives the verdict.
- C16 — reopen triggers for deferred decisions (added throughout).
- C17 — cross-machine determinism downgraded to a code-level invariant; within-machine diff is the Layer 0 gate.

**Over-Worry (dropped, cheaper substitute noted):**
- C3 — bootstrap/Wilson CIs on (Z−B) for the first number → raw n + n-floor.
- C5 — full SZZ hygiene (`-C -M`, ignore-revs, per-site confidence, sensitivity sweeps) → `git blame -w` + conservative-bias note.
- C15 — per-file harness scripts → per-criterion checks (SC0–SC6) already pin the load-bearing points.

## Net

Most important fix: the **C2 + C4 + C7 cluster** — gate the number against a mute map (coverage denominator), budget by #functions with the any-boundary-call null baseline, and run the analyzer-free repo-fit gate first. Biggest over-worry dropped: C3's statistical-power machinery. After the Act-Before-Ship edits (all cheap spec/verdict-template tightenings, no new infra) the spec is close-to-ready for `impl`.
